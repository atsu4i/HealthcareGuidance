#!/bin/bash

# 🏥 保健指導シミュレーション - Mac用起動スクリプト
# ダブルクリックするだけでアプリが起動します

# このスクリプトのディレクトリに移動
cd "$(dirname "$0")"

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🏥 保健指導シミュレーション - 起動中..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Node.jsがインストールされているか確認
if ! command -v node &> /dev/null; then
    echo "❌ エラー: Node.jsがインストールされていません"
    echo ""
    echo "以下のURLからNode.jsをインストールしてください:"
    echo "https://nodejs.org/"
    echo ""
    echo "インストール後、このスクリプトを再度実行してください。"
    echo ""
    read -p "Enterキーを押して終了..."
    exit 1
fi

echo "✅ Node.js バージョン: $(node -v)"
echo ""

# node_modulesが存在しない場合は依存関係をインストール
if [ ! -d "node_modules" ]; then
    echo "📦 初回起動: 必要なファイルをダウンロードしています..."
    echo "   (数分かかる場合があります)"
    echo ""
    npm install
    if [ $? -ne 0 ]; then
        echo ""
        echo "❌ エラー: ファイルのダウンロードに失敗しました"
        echo ""
        read -p "Enterキーを押して終了..."
        exit 1
    fi
    echo ""
    echo "✅ ダウンロード完了!"
    echo ""
fi

# 開発サーバーを起動
echo "🚀 アプリケーションを起動しています..."
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ 起動完了!"
echo ""
echo "ブラウザで以下のURLを開いてください:"
echo "👉 http://localhost:3000"
echo ""
echo "ブラウザが自動で開かない場合は、"
echo "上記のURLをコピーしてブラウザに貼り付けてください。"
echo ""
echo "終了する場合は、このウィンドウを閉じてください。"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# ブラウザを自動で開く
sleep 3
open http://localhost:3000 2>/dev/null || true

# 開発サーバー起動
npm run dev
